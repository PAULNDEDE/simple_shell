#include <stdio.h>

/**
 * main - entry of code
 * prints "programming is like building a multiligual puzle
 * * followed by new line"
 */

int main(void)
{
	puts("\"programming is like building a multilingual puzzle");

	return (0);
}
